{
  "title": "Heat capacity model",
  "subtitle": "Problem Set 1",
  "problem_set": "Problem Set 1",
  "function_name": "heat_capacity_model",
  "features": [
    "T"
  ],
  "target": "C",
  "data": {
    "train": "data/train.csv",
    "test": "data/test.csv"
  },
  "baseline": {
    "label": "Chapter 1 baseline (regularized cubic)",
    "mse": 0.6703
  },
  "blurb": "You are fitting the heat capacity <code>C(T)</code>. Fit your model in your own notebook, then report it here as a Python function <code>heat_capacity_model(T)</code> with its coefficients written out. That is the only function the scorer calls, so you are free to define others alongside it.",
  "rules": [
    "Fit on the <strong>training</strong> data only (<code>T</code> in [0,&nbsp;1]).",
    "Be honest. Score once on the test set."
  ],
  "starter": "# Your finished model.\n#\n# This page reports a model, it is not where you build one: the training\n# data is not available here. Fit in your own notebook, then write the\n# function out below with its numbers in place.\n#\n# The scorer calls heat_capacity_model(T) and nothing else, so you may\n# define as many other functions as you like alongside it. numpy is\n# available as np.\n\ndef heat_capacity_model(T):\n    # Example - the Chapter 1 baseline, and the number you have to beat.\n    # Uncomment it and you should score a test MSE of 0.6703.\n    #\n    # return 0.0233 + 1.1466*T + 0.1416*T**2 + 3.0855*T**3\n\n    return None\n",
  "submit": {
    "repo": "kalcircles/turing-circle",
    "template": "bakeoff-submission.yml"
  },
  "expose_training_data": false
}
